# Read/Write scripts for polygonZkevm contract

1. Change your own PROVIDER/PRIVATE_KEY/CONTRACT_ADDRESS in `config.json` first.

2. Try running:

```shell
node readState.js <function_name> <params>
node writeState.js <function_name> <params>
```